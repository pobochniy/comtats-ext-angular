on: push

jobs:

  Build:
    runs-on: self-hosted
    steps:
      - name: checkout 🛎️
        uses: actions/checkout@v3
      - name: publish
        run: ng build --configuration production --output-path ~/projects/web
